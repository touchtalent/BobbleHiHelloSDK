pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven {
            url mavenReadUrl
            credentials {
                username mavenReadUsername
                password mavenReadPassword
            }
        }
    }
}
rootProject.name = "BobbleHiHelloSDKSample"
include ':app'
